// Definition of styles for the form container.
const container = {
  mt: { xs: '10vh', sm: '15vh' },
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
}

const myButtonBox = {
  display: 'flex',
  justifyContent: 'center',
  mt: '10vh',
}

const fab = {
  width: '100%',
  maxWidth: '300px',
}

const backBtn = { mt: 2, color: 'secondary.main' }
const textEditingBtn = { color: 'secondary.dark' }
const textEditingBox = { display: 'flex', alignItems: 'center' }

export const sxForms = {
  container,
  myButtonBox,
  fab,
  backBtn,
  textEditingBtn,
  textEditingBox,
}
